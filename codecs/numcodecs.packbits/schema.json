{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "enum": ["numcodecs.packbits"]
        },
        "configuration": {
          "type": "object",
          "additionalProperties": false
        }
      },
      "required": ["name"],
      "additionalProperties": false
    },
    { "type": "string", "enum": ["numcodecs.packbits"] }
  ]
}
